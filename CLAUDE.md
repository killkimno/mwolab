# CLAUDE.md

## 이 문서의 위치

- 이 프로젝트의 작업 규칙 권위는 `AGENTS.md`와 그 문서가 연결하는 사양서·규칙 문서다.
- Claude는 보조 작업자다. 규칙을 새로 정의하지 않고 Codex 정의를 그대로 따른다.
- 이 문서는 규칙을 옮겨 적지 않는다. 규칙 본문을 여기에 복사하면 원본과 어긋나므로, 항상 원본 문서를 읽고 적용한다.
- Claude 작업 중 재사용할 규칙이 새로 생기거나 바뀌면 이 문서가 아니라 아래의 해당 원본 문서에 반영한다.

## 작업 전에 읽을 문서

작업 주제에 해당하는 문서를 먼저 읽는다.

| 주제 | 문서 |
| --- | --- |
| 공통 작업 규칙, 시뮬레이션/MASC/점프젯/통계 UI, 쿼크 파싱, 최적화 | `AGENTS.md` |
| 프로젝트 목적, 제공 기능, 대상 사용자, 제품 범위와 최상위 제약 | `프로젝트 사양서.md` |
| UI 원칙, 탭별 표시 규격, 슬롯/하드포인트 표시 규격 | `프로젝트 규약 에이전트.md` |
| 무기 데미지·사거리·툴팁 계산, 멕랩 장비 목록 필터 | `무기 관련 에이전트.md` |
| 정보 탭과 통계 탭의 쿼크 서머리 명칭 일치 | `쿼크 관련 에이전트.MD` |
| 구현 위키 유지보수 | `위키 관리 지침.md` |
| 게임 데이터 추출, 생성 JSON 변경 | `JSON 추출 지침.md` |
| 구현된 공통 규칙·정의·탭 안내의 상세 본문 | `wiki/mwolab-implementation-wiki.html` |

## 충돌 처리

- 제품이 무엇을 제공해야 하는지 판단할 때는 사용자 요청과 `프로젝트 사양서.md`를 기준으로 한다. 현재 코드가 다르다는 이유만으로 사양을 현재 구현에 맞춰 바꾸지 않는다.
- 현재 구현 동작과 게임 수치를 판단할 때는 현재 코드와 새로 추출한 게임 데이터를 기준으로 한다.
- 구현·계산 방법은 해당 규칙 문서를 따른다. 규칙과 코드가 다르면 의도된 변경인지 스테일 문서인지 확인해 사용자에게 알린다.
- 구현 위키는 현재 구현을 설명하는 현황 문서다. 코드와 어긋나면 현재 동작을 확인한 뒤 위키를 갱신한다.

## 작업 방식

- 기존 작업자의 수정이 있는 파일은 되돌리지 않고 필요한 부분만 좁게 고친다.
- 검증 명령과 범위는 `package.json` 및 작업 주제에 해당하는 지침을 확인한다.
- 사용자와의 대화는 한국어로 한다.
