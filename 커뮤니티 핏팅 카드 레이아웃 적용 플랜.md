# 커뮤니티 핏팅 카드 레이아웃 적용 플랜

## 1. 목표

핏팅 브라우저의 목록 카드를 더 쉽게 훑을 수 있도록 정보 위계를 재배치한다. 실제 로드아웃에서 파생한 대표 무장을 태그 형태로 최대 4개 표시하고, 자동 태그가 여러 개여도 좋아요·작성자·날짜·하드포인트의 위치가 흔들리지 않게 한다.

이 변경은 목록 카드에만 적용한다. 상세 패널의 전체 장착 무기 목록과 좋아요 실행 버튼은 기존 동작을 유지한다.

## 2. 확정된 표시 요구사항

- 카드 제목은 기존처럼 `멕 이름 태그 → 핏팅 제목` 순서를 유지한다.
- 자동 태그는 현재 계산 결과를 모두 표시하며 개수에 따라 독립적으로 줄바꿈할 수 있다.
- 대표 무장은 자동 태그와 별도 영역에 무기 칩만 표시하고 `대표무기` 라벨은 표시하지 않는다.
- 대표 무장은 실제 장착 무기의 원본 표시명을 사용한다.
- 대표 무기는 아이콘과 장착 수량 없이 태그 모양의 텍스트 칩으로 표시한다.
- 대표 무기명은 최대 4개까지만 한 줄에 표시한다.
- 서로 다른 실제 장착 무기가 4종을 초과해도 상위 4종만 표시하고 `[+]` 같은 추가 표시는 만들지 않는다.
- 공개·내 게시물의 좋아요 아이콘과 집계 수는 메크 썸네일 바로 아래에 표시한다.
- 작성자와 날짜는 본문 하단 왼쪽에 표시하며 좋아요 수는 이 행에서 제거한다.
- E·M·B·AMS 하드포인트 배지는 공용 마크업·색·순서를 유지하고 카드 하단 오른쪽에 표시한다.
- 하드포인트 값은 기체 최대치가 아니라 현재처럼 실제 장착 무기의 명시적 타입별 개수다.
- 값이 0인 하드포인트 타입은 계속 숨긴다.
- 로컬 핏팅은 좋아요와 작성자를 표시하지 않고 날짜만 표시하는 기존 기능 경계를 유지한다.

## 3. 확정된 데이터 규칙

### 3.1 대표 무기 4종의 선정·정렬 기준

- 실제 장착 무기를 장비 ID별로 합산한다.
- 각 무기의 `장비 중량 × 장착 수량`으로 총 톤수를 계산한다.
- 총 톤수 내림차순의 상위 4종을 대표 무장으로 사용한다.
- 총 톤수가 같으면 로드아웃에서 처음 집계된 순서를 유지한다.
- 화력·이름·제목에서는 대표성을 추측하지 않는다.

### 3.2 AMS의 대표 무기 후보 포함 여부

AMS도 실제 장착 무기이므로 대표 무기 후보에 포함한다. 공격 무장 시뮬레이션에서 제외되는 기존 계산 경계와 하드포인트 배지 표시는 그대로 유지한다.

## 4. 현재 구현 근거

| 영역 | 현재 구현 | 계획상 역할 |
|---|---|---|
| 로드아웃 분석 | `public/app.js`의 `analyzeMwoCode()` | 실제 장착 무기·태그·하드포인트를 한 번 계산하고 브라우저 메모리에 캐시한다. |
| 장착 무기 집계 | `public/app.js`의 `communityInstalledWeaponSummary()` | 장비 ID별 고유 무기명, 수량, 명시적 하드포인트 타입을 제공한다. |
| 목록 카드 HTML | `public/firebase-community.js`의 `fittingCardHtml()` | 썸네일/좋아요 묶음, 대표무기 칩, 본문 메타 정보와 하드포인트 배치를 렌더링한다. |
| 자동 태그 | `public/firebase-community.js`의 `tagHtml()` | 기존 자동 태그 전체 렌더링을 유지한다. |
| 하드포인트 | `public/firebase-community.js`의 `hardpointsHtml()` | E→M→B→AMS 순서와 공용 `hardpoint-chip` 마크업을 그대로 재사용한다. |
| 카드 스타일 | `public/styles.css`의 `.community-fitting-card` 및 `.community-card-*` | 새 그리드 행, 썸네일 하단 좋아요, 대표무기 한 줄, 하단 정렬과 줄임표를 정의한다. |
| 계약 테스트 | `tests/firebase-community-config.test.js` | 카드 마크업·CSS·다국어·하드포인트 및 좋아요 위치 계약을 검증한다. |
| 계산 테스트 | `tests/mechlab-formulas.test.js` | 실제 장착 무기 집계와 대표 무기 경계 조건이 공통 계산을 훼손하지 않는지 검증한다. |

Firestore 문서, 로컬 저장 레코드, `public/data/*.json`에는 대표 무기나 자동 태그를 새로 저장하지 않는다. 모든 값은 기존 로드아웃 코드 분석 결과에서 파생하고 `communityFittingAnalysisCache`를 재사용한다.

## 5. 구현 단계

### 단계 A — 대표 무기 표시 모델 확정

- 확정된 규칙을 작은 순수 헬퍼로 구현한다.
- 입력은 `analysis.weapons`, 출력은 총 톤수 순으로 정렬한 최대 4개의 표시 항목으로 제한한다.
- 표시명은 기존 `display_name || name || id` 경로를 사용하고 이름이나 타입을 문자열 규칙으로 추론하지 않는다.
- 대표 무기 칩은 기존 명시적 `type`을 이용해 에너지·미사일·발리스틱·AMS 색을 적용한다.

### 단계 B — 목록 카드 마크업 재구성

- `fittingCardHtml()`의 단독 `<img>`를 `썸네일 + 좋아요 집계` 전용 열 래퍼로 교체한다.
- 카드 전체가 하나의 `<button>`이므로 썸네일 아래 좋아요는 집계 표시만 사용한다. 중첩 버튼을 만들지 않는다.
- 실제 좋아요 실행은 상세 패널 하단의 기존 `data-community-like` 버튼을 유지한다.
- 본문은 다음 순서로 렌더링한다.

  1. 멕 이름 태그와 핏팅 제목
  2. 기존 자동 태그 영역
  3. 별도 라벨 없는 최대 4개의 대표 무기 칩
  4. 작성자와 날짜

- 로컬 핏팅은 썸네일 아래 좋아요 영역과 작성자 문구를 렌더링하지 않는다.
- 하드포인트 컨테이너는 카드의 독립된 마지막 열 또는 하단 셀로 유지해 본문 태그 수와 무관하게 오른쪽 아래에 정렬한다.
- 상세 패널의 전체 무기 목록, 수량, 타입 색, 좋아요·삭제·공유·적용 버튼은 변경하지 않는다.

### 단계 C — 다국어와 접근성

- 장비 고유 표시명은 번역하지 않고 추출 데이터의 영문명을 유지한다.
- 긴 제목과 무기명은 카드 폭을 넘지 않도록 말줄임표를 사용한다.

### 단계 D — CSS 레이아웃

- `.community-fitting-card`의 열 구조를 다음 책임으로 분리한다.

  - 왼쪽: 고정 폭 썸네일과 좋아요 집계
  - 가운데: 제목, 자동 태그, 대표 무기, 작성자·날짜
  - 오른쪽 하단: 공용 하드포인트 배지

- 기존 `.community-fitting-card > img` 직접 선택자를 새 썸네일 래퍼 구조에 맞게 변경한다.
- 썸네일은 좋아요를 위한 공간을 제외한 영역에 맞추고 카드 높이 전체를 강제로 차지하지 않게 한다.
- 자동 태그는 `flex-wrap`을 유지한다.
- 대표 무기 행은 `nowrap`, `min-width: 0`, 개별 이름 말줄임표를 사용해 한 줄을 보장한다.
- 하드포인트에는 기존 `align-content: end`, `justify-content: flex-end` 의도를 유지한다.
- 900px 이하 브라우저 배치에서도 카드 내부 세 영역이 겹치지 않는지 확인하고 필요한 카드 전용 미디어 규칙만 추가한다.

### 단계 E — 권위 문서 동기화

- `프로젝트 규약 에이전트.md`의 기존 커뮤니티 카드 규칙을 새 배치와 일치시킨다.

  - 좋아요 집계는 썸네일 아래
  - 작성자·날짜는 본문 하단 왼쪽
  - 대표 무기는 총 톤수 순의 태그형 최대 4개이며 초과 표시 없음
  - 자동 태그 다중 표시
  - 공용 하드포인트는 하단 오른쪽

- 구현 완료 후 `wiki/mwolab-implementation-wiki.html`의 현재 카드 설명과 실제 함수·클래스 구조를 동기화한다.
- 제품 범위나 계산 공식은 바뀌지 않으므로 `프로젝트 사양서.md`, `무기 관련 에이전트.md`, 생성 JSON 문서는 변경하지 않는다.

## 6. 테스트 계획

### 커뮤니티 UI·설정 계약

명령:

```text
npm run test:community
```

검증 항목:

- 목록 카드가 썸네일 아래에 좋아요 집계를 한 번만 렌더링한다.
- 본문 메타 행에는 좋아요가 없고 작성자·날짜만 있다.
- 로컬 카드에는 좋아요와 작성자가 없다.
- 대표 무기 라벨과 칩은 KR/EN 문구를 모두 가진다.
- 대표 무기 칩에 아이콘과 `×수량`이 없다.
- 후보 수와 관계없이 대표 무장은 최대 4종이고 `[+]`가 없다.
- 자동 태그와 대표 무기 영역이 별도 컨테이너다.
- E·M·B·AMS는 기존 공용 마크업과 순서로 하단 오른쪽에 남는다.

### 장착 무기·하드포인트 파생값

명령:

```text
npm run test:formulas
```

검증 항목:

- 같은 장비 ID의 중복 장착은 대표 후보에서 한 종류로 취급한다.
- 실제 장착 무기만 후보가 되고 탄약·일반 장비는 포함되지 않는다.
- 무기 표시명과 명시적 하드포인트 타입을 기존 데이터에서 사용한다.
- 총 톤수 대표 선정 순서와 AMS 포함 규칙을 0·1·3·4종 경계에서 검증한다.
- 자동 태그와 하드포인트 계산 결과가 기존 테스트와 동일하다.

이 변경은 커뮤니티 카드와 그 입력 요약에 한정되므로 `npm test`와 `npm run test:all`은 실행하지 않는다. 다른 영역을 함께 변경하게 되면 영향 범위를 다시 평가한다.

## 7. 수동 확인 체크리스트

로컬 미리보기 서버의 실행·접속·검증은 사용자가 직접 관리한다. 에이전트는 별도 요청 없이 서버를 시작하거나 상태를 확인하지 않는다.

- 공개 카드에서 좋아요가 썸네일 중앙 아래에 보인다.
- 로컬 카드에서 좋아요가 사라져도 썸네일과 본문 정렬이 어색하지 않다.
- 자동 태그가 1개, 4개, 더 많은 경우에도 대표 무기·작성자·하드포인트가 겹치지 않는다.
- 대표 후보가 4종이면 무기 칩 4개만, 5종 이상이어도 총 톤수 상위 무기 칩 4개만 보인다.
- 긴 영문 무기명에서도 대표 무기 행은 한 줄을 유지한다.
- E·M·B·AMS 중 0인 타입은 숨고 나머지는 하단 오른쪽에 정렬된다.
- 선택·hover·focus-visible 상태와 카드 전체 클릭 동작이 유지된다.
- 공개·내 게시물·로컬 세 탭의 카드 높이와 정렬이 일관된다.

## 8. 완료 조건

- 3절의 총 톤수·AMS 규칙이 구현과 테스트에 반영되어 있다.
- 확정 요구사항이 목록 카드에 적용되어 있다.
- DB 스키마와 생성 JSON에 파생 표시값을 추가하지 않았다.
- `npm run test:community`와 `npm run test:formulas`가 통과한다.
- 실행한 두 테스트가 각각 어떤 영향을 검증했는지 완료 보고에 명시한다.
- UI 규약과 구현 위키가 최종 코드와 일치한다.
- 미리보기 서버를 에이전트가 임의로 실행하거나 검사하지 않았다.
