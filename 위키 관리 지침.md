# 구현 위키 관리 지침

- 프로젝트 동작을 구현하거나 변경하기 전에 `wiki/mwolab-implementation-wiki.html`에서 관련 공통 규칙, 정의, 탭 안내, JSON 파싱 규칙 또는 MechLab 규칙을 확인한다.
- 구현 위키와 현재 코드 또는 새로 추출한 게임 데이터가 충돌하면 코드와 추출 데이터를 기준으로 삼는다. 어느 쪽을 변경하기 전에 실제 동작을 확인한다.
- 사용자 요청에 따른 구현이나 코드 변경을 마친 후, 재사용할 구현 규칙을 새로 만들거나 변경하거나 명확히 했는지 판단한다. 위키 갱신이 필요하면 별도 요청을 기다리지 않고 같은 작업에서 즉시 갱신한다.
- 위키를 변경할 때는 현재 구현을 먼저 검증하고, 탭 안내, 공통 규칙, 정의, JSON 파서 규칙, MechLab 규칙 및 ETC 안내를 코드와 일치시킨다.
- 구현 위키는 별도의 내부 HTML 문서다. 사용자가 명시적으로 요청하지 않는 한 애플리케이션에 위키 링크를 추가하지 않는다.
