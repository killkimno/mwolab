# 커뮤니티 핏팅 상세 헤더 작성자·좋아요 적용 플랜

## 1. 목표

핏팅 브라우저에서 목록 카드를 눌렀을 때 표시되는 상세 화면의 헤더를 재구성한다. 작성자와 업데이트 날짜는 수동으로 읽는 출처 정보로 묶고, 좋아요는 헤더 오른쪽의 독립된 상태·동작 영역으로 분리해 정보와 기능의 역할을 명확하게 한다.

이 계획은 상세 패널의 헤더와 기존 상세 좋아요 버튼에만 적용한다. 목록 카드 배치는 `커뮤니티 핏팅 카드 레이아웃 적용 플랜.md`에서 별도로 다룬다.

## 2. 확정 UI 구조

상세 헤더는 좌우 2열 구조를 사용한다.

### 왼쪽 정보 영역

1. 멕 이름 태그와 핏팅 제목
2. 현재 자동 태그 전체
3. 작성자와 업데이트 날짜

작성자·날짜 행은 다음 위계를 사용한다.

- `작성자`는 낮은 명도의 보조 라벨로 표시한다.
- 닉네임은 라벨보다 밝고 굵게 표시한다.
- 작성자와 업데이트 날짜 사이에는 작은 구분점 또는 얇은 구분선을 둔다.
- `업데이트` 라벨과 날짜는 작성자명보다 낮은 명도로 표시한다.
- Google 프로필 사진, 표시 이름, 이메일은 사용하지 않는다.
- 닉네임이 없으면 기존 규칙대로 `Pilot`을 표시한다.

### 오른쪽 좋아요 영역

- 엄지손가락 아이콘과 현재 집계 수를 하나의 여유 있는 크기의 컨트롤에 표시한다.
- `좋아요`와 `좋아요 취소` 문구는 화면에 표시하지 않고 현재 상태에 맞는 접근 가능한 이름과 툴팁으로 제공한다.
- 컨트롤은 헤더 오른쪽에 고정하고 제목·자동 태그·작성자 행과 겹치지 않게 한다.
- 좋아요하지 않은 상태는 어두운 배경과 청회색 테두리를 사용한다.
- 좋아요한 상태는 현재 `.liked` 의미를 유지하되 선택 상태가 분명한 강조색을 사용한다.
- 집계 수는 0 이상의 정수만 표시하고 기존 `Math.max(0, Number(record.likeCount) || 0)` 방어 처리를 유지한다.

## 3. 탭·인증 상태별 동작

| 상태 | 작성자 | 업데이트 날짜 | 헤더 좋아요 | 하단 좋아요 |
|---|---|---|---|---|
| 공개 탭 + 로그인 | 표시 | 표시 | 클릭 가능한 버튼 | 제거 |
| 공개 탭 + 비로그인 | 표시 | 표시 | 같은 위치의 비활성 버튼 | 제거 |
| 내가 올린 핏팅 | 표시 | 표시 | 같은 모양의 읽기 전용 집계 | 없음 |
| 로컬 핏팅 | 숨김 | 표시 | 숨김 | 없음 |

- 공개 탭의 로그인 사용자는 헤더 버튼으로 기존 `toggleLike(id)` 경로를 실행한다.
- 비로그인 상태는 기존 기능 범위를 유지해 좋아요를 실행하지 않는다. 이 작업에서 로그인 유도 팝업을 새로 만들지 않는다.
- 내가 올린 핏팅에서는 자기 게시물의 좋아요 집계만 보여주고 버튼으로 렌더링하지 않는다.
- 로컬 핏팅에는 작성자와 좋아요 데이터가 없으므로 빈 자리나 비활성 컨트롤을 만들지 않는다.

## 4. 현재 구현 근거와 변경 파일

| 파일 | 현재 역할 | 변경 내용 |
|---|---|---|
| `public/firebase-community.js` | `fittingDetailHtml()`이 상세 제목·태그·메타·하단 버튼을 렌더링한다. | 상세 헤더를 좌우 영역으로 나누고 좋아요 버튼/집계를 오른쪽에 렌더링한다. 기존 하단 좋아요 버튼은 제거한다. |
| `public/firebase-community.js` | `toggleLike()`, `updateLikeViews()`, `ensureLikeState()`가 좋아요 상태와 집계를 갱신한다. | 데이터·트랜잭션 경로는 변경하지 않고 새 헤더 버튼이 같은 경로를 사용하게 한다. |
| `public/firebase-community.js` | `COPY`가 한국어·영어 작성자, 좋아요, 업데이트 문구를 제공한다. | 기존 번역을 재사용하고 필요하면 좋아요 취소용 접근성 문구를 KR/EN으로 추가한다. |
| `public/styles.css` | `.community-detail-title`, `.community-detail-meta`, `.community-detail-like*`가 상세 헤더와 하단 좋아요를 꾸민다. | 헤더 그리드, 메타 강조, 오른쪽 좋아요 컨트롤과 좁은 폭 대응 스타일로 교체한다. |
| `tests/firebase-community-config.test.js` | 상세 좋아요가 하단에 있고 아이콘만 표시된다는 계약을 검사한다. | 새 헤더 위치·텍스트·수치·상태·중복 제거 계약으로 갱신한다. |
| `프로젝트 규약 에이전트.md` | 목록·상세·출처의 좋아요 표시 범위를 정의한다. | 상세 헤더는 여유 있는 아이콘·집계 컨트롤을 사용하고 동작 문구는 접근 가능한 이름으로만 제공한다고 명시한다. |
| `wiki/mwolab-implementation-wiki.html` | 현재 상세 헤더와 하단 작업 버튼 구조를 설명한다. | 구현 완료 후 새 헤더 좋아요 위치와 상태별 렌더링을 실제 코드와 맞춘다. |

Firestore 규칙, 좋아요 서브컬렉션, 공개 핏팅 문서 스키마, 생성 JSON, 장착 무기·상세 수치 계산은 변경하지 않는다.

## 5. 구현 단계

### 단계 A — 상세 헤더 마크업 재구성

- `fittingDetailHtml()`의 `<header>` 내부를 다음 두 영역으로 분리한다.

  - `.community-detail-heading-main`: 제목, 자동 태그, 작성자·날짜
  - `.community-detail-like-area`: 공개 좋아요 버튼 또는 내 게시물 읽기 전용 집계

- 왼쪽 영역의 제목과 자동 태그 렌더링 순서는 유지한다.
- 기존 `.community-detail-meta` 안에 있던 좋아요 집계를 제거한다.
- 작성자와 날짜는 각각 독립된 요소로 유지해 긴 닉네임과 언어별 문구 폭을 제어할 수 있게 한다.
- 로컬 핏팅은 기존처럼 날짜만 렌더링한다.

### 단계 B — 좋아요 동작을 헤더로 이동

- 공개 탭에서는 헤더 컨트롤에 기존 `data-community-like="record.id"`를 부여한다.
- 로그인 상태의 공개 탭만 활성화하고 현재 `canLike` 조건을 그대로 사용한다.
- 기존 상세 하단의 `data-community-like` 버튼을 제거해 같은 상세 화면에 좋아요 동작이 두 번 노출되지 않게 한다.
- `elements.content`의 이벤트 위임과 `toggleLike()` 트랜잭션은 변경하지 않는다.
- `updateLikeViews()`가 재렌더링할 때 새 헤더의 수치와 liked 상태가 함께 갱신되는지 확인한다.
- 내 게시물 탭은 `<button>`이 아닌 읽기 전용 요소로 같은 위치에 집계만 표시한다.
- 공개 탭 비로그인 상태는 비활성 `<button>`을 사용해 레이아웃과 접근성 의미를 유지한다.

### 단계 C — 접근성과 상태 표현

- 공개 좋아요 버튼에 현재 상태를 나타내는 `aria-pressed="true|false"`를 추가한다.
- 접근 가능한 이름은 현재 상태에 따라 `좋아요` 또는 `좋아요 취소` 의미를 제공하고 영어 문구도 함께 추가한다.
- 화면에 보이는 집계 수가 바뀔 때 버튼 전체의 접근 가능한 이름에도 최신 수치를 반영한다.
- 아이콘은 기존 `likeIconHtml()` SVG를 재사용하고 `aria-hidden="true"`를 유지한다.
- 내 게시물의 읽기 전용 집계에는 버튼 역할이나 `aria-pressed`를 부여하지 않는다.
- 비활성 버튼은 키보드로 실행되지 않고 현재 로그인 요구 정책을 유지한다.

### 단계 D — CSS 레이아웃과 반응형 처리

- 상세 헤더를 `minmax(0, 1fr) auto`의 2열 그리드로 구성한다.
- 오른쪽 좋아요 영역은 `align-self: center` 또는 헤더 상단 정렬 중 시안과 실제 높이에 맞는 값을 사용하되 제목과 같은 중요도로 커 보이지 않게 한다.
- 작성자·날짜 메타 행은 flex-wrap을 허용해 긴 닉네임과 영어 문구에서 잘리지 않게 한다.
- 닉네임만 별도 클래스로 밝기와 굵기를 높인다.
- 좋아요 컨트롤은 아이콘과 집계가 한 줄에 유지되도록 `white-space: nowrap`을 적용하고 충분한 최소 너비와 높이를 준다.
- liked, hover, focus-visible, disabled 상태를 각각 구분한다.
- 좁은 상세 폭에서는 오른쪽 좋아요 영역을 헤더의 다음 행 오른쪽 또는 전체 폭 끝으로 내려 제목과 겹치지 않게 한다.
- 상세 카드의 전체 높이, 본문 스크롤 영역, 하단 삭제·공유·적용 버튼 고정 구조는 유지한다.

### 단계 E — 하단 작업 영역 정리

- 상세 footer에서는 좋아요 버튼만 제거한다.
- 삭제, 공유하기, 이 핏팅 적용 버튼의 기존 노출 조건과 순서를 유지한다.
- 좋아요 버튼 제거 후 남은 버튼이 계속 오른쪽 정렬되고 불필요한 빈 공간이 생기지 않는지 확인한다.
- 목록 카드와 적용한 공개 핏팅 출처 패널의 좋아요 UI는 이 계획의 범위에서 변경하지 않는다.

### 단계 F — 문서 동기화

- `프로젝트 규약 에이전트.md`에 상세 화면 전용 규칙을 기록한다.

  - 작성자·업데이트는 헤더 왼쪽의 수동 메타 정보
  - 좋아요 버튼 또는 집계는 헤더 오른쪽
  - 공개 로그인만 동작 가능
  - 내가 올린 핏팅은 읽기 전용 집계
  - 로컬은 좋아요·작성자 없음
  - 상세 하단에는 좋아요 버튼을 중복 표시하지 않음

- 목록·상세·출처 패널마다 아이콘과 집계 수를 표시하는 범위를 구분해 문구가 충돌하지 않도록 한다.
- 구현 후 `wiki/mwolab-implementation-wiki.html`의 상세 헤더·footer 설명을 실제 마크업과 일치시킨다.

## 6. 테스트 계획

직접 영향 범위는 커뮤니티 상세 UI와 기존 좋아요 이벤트 경로이므로 다음 테스트만 실행한다.

```text
npm run test:community
```

검증 항목:

- 상세 헤더가 왼쪽 정보 영역과 오른쪽 좋아요 영역을 가진다.
- 공개 로그인 상태는 헤더에 `data-community-like` 버튼을 한 번만 렌더링한다.
- 상세 footer에는 `data-community-like`가 없다.
- 공개 비로그인 상태의 헤더 좋아요 버튼은 비활성이다.
- 내가 올린 핏팅은 작성자·날짜·읽기 전용 좋아요 집계를 표시하고 좋아요 버튼은 표시하지 않는다.
- 로컬 핏팅은 날짜만 표시하고 작성자·좋아요 영역을 만들지 않는다.
- 좋아요 컨트롤은 아이콘과 0 이상의 집계 수만 화면에 표시하고 KR/EN 동작 문구는 접근 가능한 이름으로 제공한다.
- liked 상태 클래스와 `aria-pressed`가 `record.liked`를 반영한다.
- 기존 `toggleLike()`, `updateLikeViews()`, `ensureLikeState()` 경로가 유지된다.
- 삭제·공유·적용 버튼의 탭별 노출 조건이 유지된다.
- CSS가 2열 헤더, 메타 줄바꿈, 좋아요 한 줄, liked/focus/disabled 상태를 정의한다.

좋아요 트랜잭션, Firestore 규칙, 장비 계산을 변경하지 않으므로 공식·데이터·관리자 테스트와 전체 테스트는 실행하지 않는다. 구현 중 해당 영역까지 변경되면 영향 범위를 다시 평가한다.

## 7. 수동 확인 체크리스트

로컬 미리보기 서버의 실행·접속·검증은 사용자가 직접 관리한다. 에이전트는 별도 요청 없이 서버를 시작하거나 상태를 확인하지 않는다.

- 상세 제목과 자동 태그가 현재와 같은 순서로 보인다.
- 작성자 라벨보다 닉네임이 밝고 쉽게 구분된다.
- 업데이트 날짜는 작성자보다 낮은 우선순위로 보인다.
- 좋아요 컨트롤은 헤더 오른쪽에서 제목보다 덜 강조되지만 클릭 가능함이 분명하다.
- 좋아요 실행 직후 아이콘 상태와 집계 수가 함께 갱신된다.
- 공개 비로그인, 공개 로그인, 내가 올린 핏팅, 로컬 탭에서 표의 상태대로 보인다.
- 긴 닉네임과 영어 UI에서도 제목·좋아요가 겹치지 않는다.
- 좁은 폭에서도 좋아요 컨트롤이 자연스럽게 다음 행으로 이동한다.
- 장착 무기, 상세 정보, 삭제·공유·적용 영역은 변경 전과 동일하게 동작한다.

## 8. 완료 조건

- 작성자·업데이트와 좋아요의 역할이 상세 헤더에서 시각적으로 분리되어 있다.
- 좋아요 실행 위치가 헤더에 하나만 존재하고 하단 중복 버튼이 제거되어 있다.
- 공개·내 게시물·로컬 상태별 표시와 권한이 기존 Firebase 동작을 보존한다.
- 새 사용자 문구가 한국어와 영어로 제공되고 버튼 상태가 접근 가능하다.
- `npm run test:community`가 통과하고 검증한 상세 UI·좋아요 동작 범위를 완료 보고에 명시한다.
- UI 규약과 구현 위키가 최종 코드와 일치한다.
- 미리보기 서버를 에이전트가 임의로 실행하거나 검사하지 않았다.
